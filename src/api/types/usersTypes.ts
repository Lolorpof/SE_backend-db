type formattedSingleUserRegisterType = {
  firstName: string;
  lastName: string;
  email: string;
  hashedPassword: string;
};

type formattedCompanyRegisterType = {
  officialName: string;
  email: string;
  hashedPassword: string;
};
